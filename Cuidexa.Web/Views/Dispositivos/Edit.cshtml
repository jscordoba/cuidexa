@model CuentaDispositivoEditDto
@{
    var roles = (RolEmpleado[])ViewBag.Roles;
}
<a href="/Dispositivos" class="d-inline-block mb-2 text-decoration-none">&larr; Volver a Dispositivos</a>
<h4 class="mb-3">Editar tablet</h4>
<form method="post" class="bg-white p-3 p-md-4 border rounded" style="max-width: 480px;">
    @Html.AntiForgeryToken()

    @if (!ViewData.ModelState.IsValid)
    {
        <div class="alert alert-danger">
            @foreach (var e in ViewData.ModelState.Values.SelectMany(v => v.Errors))
            {
                <div>@e.ErrorMessage</div>
            }
        </div>
    }

    <div class="mb-3">
        <label class="form-label">Nombre de la tablet</label>
        <input name="Nombre" class="form-control" value="@Model.Nombre" required />
    </div>
    <div class="mb-3">
        <label class="form-label">Departamento</label>
        <select name="Rol" class="form-select">
            @foreach (var rol in roles)
            {
                <option value="@rol" selected="@(rol == Model.Rol)">@rol</option>
            }
        </select>
    </div>
    <div class="mb-3">
        <label class="form-label">Nueva contraseña</label>
        <input type="password" name="NuevaPassword" class="form-control" minlength="8" placeholder="Dejar en blanco para no cambiarla" />
    </div>

    <div class="d-flex flex-column flex-sm-row gap-2">
        <button type="submit" class="btn btn-primary">Guardar cambios</button>
        <a href="/Dispositivos" class="btn btn-outline-secondary">Cancelar</a>
    </div>
</form>
