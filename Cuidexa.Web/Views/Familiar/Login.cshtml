@{
    Layout = null;
}
<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Cuidexa · Portal de familiares</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" />
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css" />
    <link rel="stylesheet" href="~/css/site.css" asp-append-version="true" />
    <style>
        .pantalla-acceso {
            min-height: 100vh;
            min-height: 100dvh;
            display: flex;
            align-items: center;
            justify-content: center;
            background: #f8f9fa;
            padding: 1.75rem 1rem;
        }
        .tarjeta-acceso { width: 100%; max-width: 380px; }
        .logo-familiar {
            width: 4.25rem; height: 4.25rem; border-radius: 50%;
            background: linear-gradient(135deg, #06b6d4, #4f46e5);
            display: flex; align-items: center; justify-content: center;
            color: #fff; font-size: 2rem; margin: 0 auto 1rem;
        }
    </style>
</head>
<body>
    <div class="pantalla-acceso">
        <div class="tarjeta-acceso">
            <div class="text-center mb-4">
                <div class="logo-familiar"><i class="bi bi-people-fill"></i></div>
                <h1 class="h4 mb-1 fw-bold">Portal de familiares</h1>
                <p class="text-muted small mb-0">Documentos e incidencias resueltas de tu familiar</p>
            </div>

            <div class="card border-0 shadow-sm p-4">
                @if (!ViewData.ModelState.IsValid)
                {
                    <div class="alert alert-danger py-2 small mb-3">
                        @foreach (var e in ViewData.ModelState.Values.SelectMany(v => v.Errors))
                        {
                            <div>@e.ErrorMessage</div>
                        }
                    </div>
                }
                <form method="post">
                    @Html.AntiForgeryToken()
                    <div class="mb-3">
                        <label class="form-label small text-muted">Email</label>
                        <input type="email" name="email" class="form-control" required autofocus autocomplete="username" />
                    </div>
                    <div class="mb-4">
                        <label class="form-label small text-muted">Contraseña</label>
                        <input type="password" name="password" class="form-control" required autocomplete="current-password" />
                    </div>
                    <button type="submit" class="btn btn-primary w-100 py-2 fw-semibold">Entrar</button>
                </form>
            </div>
            <p class="text-center text-muted small mt-3 mb-0">
                ¿No tienes acceso todavía? Pide al centro que te dé de alta desde la ficha de tu familiar.
            </p>
        </div>
    </div>
</body>
</html>
